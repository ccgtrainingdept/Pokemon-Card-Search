{
  "name": "pokemon-card-search",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 3000",
    "dev:win": "vite --host 0.0.0.0 --port 3000",
    "dev:wrangler": "wrangler pages dev dist --port 3000 --host 0.0.0.0",
    "build": "vite build",
    "preview": "wrangler pages dev dist --port 3000",
    "start": "npm run build && npm run dev:wrangler",
    "start:dev": "npm run dev:win",
    "deploy": "npm run build && wrangler pages deploy dist",
    "deploy:prod": "npm run build && wrangler pages deploy dist --project-name pokemon-card-search",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings",
    "install:win": "npm install --legacy-peer-deps",
    "setup:win": "npm run install:win && npm run build",
    "test:local": "echo Testing localhost:3000... && timeout 5 > nul && echo Please check http://localhost:3000 in your browser"
  },
  "dependencies": {
    "hono": "^4.9.6"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "4.20250705.0",
    "@hono/vite-build": "^1.2.0",
    "@hono/vite-cloudflare-pages": "^0.4.2",
    "@hono/vite-dev-server": "^0.18.2",
    "typescript": "^5.0.0",
    "vite": "^6.3.5",
    "wrangler": "^4.4.0"
  }
}
